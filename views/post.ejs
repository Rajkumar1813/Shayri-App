<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Post a Shayari - Shayari Site</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/styles.css" />

  <style>
    body {
      background: #f7f9fc;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }
    .post-container {
      background: #fff;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 4px 18px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      width: 100%;
    }
    .notification {
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>

  <div class="post-container">

    <h2 class="mb-4 text-center"><i class="bi bi-pencil-square"></i> Post New Shayari</h2>

    <!-- Notification Area -->
    <% if (success && success.length) { %>
      <div class="alert alert-success alert-dismissible fade show notification" role="alert">
        <i class="bi bi-check-circle-fill"></i>
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="alert alert-danger alert-dismissible fade show notification" role="alert">
        <i class="bi bi-exclamation-triangle-fill"></i>
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <form action="/post" method="post" novalidate>
      <div class="mb-3">
        <textarea
          name="content"
          rows="6"
          class="form-control"
          placeholder="Write your beautiful Shayari here..."
          required
          autofocus
        ></textarea>
      </div>

      <button type="submit" class="btn btn-primary w-100">
        <i class="bi bi-send"></i> Post Shayari
      </button>
    </form>

    <p class="text-center mt-4 mb-0">
      <a href="/" class="text-decoration-none"><i class="bi bi-arrow-left-circle"></i> Back to Home</a>
    </p>

  </div>

  <!-- Bootstrap Bundle JS (for dismissible alerts) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
